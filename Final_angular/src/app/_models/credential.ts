
import {PAType} from './PAType';
import {User} from './user';

export class Credential {

  owner: string;
  domain: string;
  username: string;
  password: string;
  expiry: Date;
  createdDate: Date;
  access: string[];
  }

